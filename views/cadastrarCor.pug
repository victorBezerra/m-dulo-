html
  head
    title= message
    script.
      function validar(){
        const descricao = formulario.descricao.value;
        if(descricao==""){
          alert("Campo descricao vazio!")
          return false
        }
      }
      
  body
    h1= message
    form(method ="post", action="/cadastrar/cor", id="formulario", onsubmit="return validar();")
      input(type="text", name="descricao")
      input(type="submit", value="save")
